{
  "name": "get-calendar-dates",
  "version": "1.0.0",
  "description": "Get all ISO calendar dates in a string",
  "repository": "shinnn/get-calendar-dates",
  "author": "Shinnosuke Watanabe (https://github.com/shinnn)",
  "scripts": {
    "bower": "bower install --production",
    "pretest": "eslint --fix --config @shinnn browser.js index.js test.js && ${npm_package_scripts_bower}",
    "test": "node --strong_mode --throw-deprecation --track-heap-objects test.js | tap-spec",
    "coverage": "node --strong_mode --throw-deprecation --track-heap-objects node_modules/.bin/istanbul cover -x=bower_components/*/*.js test.js"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "keywords": [
    "match",
    "string",
    "search",
    "date",
    "year",
    "month",
    "day",
    "calendar",
    "iso",
    "iso8601",
    "browser",
    "client-side"
  ],
  "dependencies": {
    "calendar-date-regex": "^1.0.1"
  },
  "devDependencies": {
    "@shinnn/eslint-config": "^1.4.0",
    "bower": "^1.6.8",
    "eslint": "^1.10.3",
    "istanbul": "^0.4.1",
    "require-bower-files": "^3.0.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.2.2"
  }
}
